{
  "$schema": "../gen/schemas/capability.json",
  "identifier": "default",
  "description": "default capability",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-all",
    "autostart:default",
    "os:allow-all"
  ]
}